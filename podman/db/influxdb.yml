version: '3'
services:
  influxdb:
    image: influxdb:2
    restart: always
    network_mode: "host"
    volumes:
      - /opt/influxdb2/data:/var/lib/influxdb2
      - /opt/influxdb2/config:/etc/influxdb2
