version: "3"
services:
  telegraf:
    image: telegraf
    restart: always
    network_mode: "host"
    command:
      - "--config-directory"
      - "/etc/telegraf"
    volumes:
      - /etc/telegraf:/etc/telegraf
